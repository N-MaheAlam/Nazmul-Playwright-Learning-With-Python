"""
Claude Code:

is nothing but an AI agent that can have access in your coding files, and we can use it to
 manipulate, refactor our code, write new code, and solutions for error.


and one more things comes into picture is the MCP,
with claude code, we can find errors, built codes, structures in our project. But if we want to open
the browser and to perform some steps such as give login credentials, click button, find the product,
claude code does not have this permission. With the help of MCP-playwright, we can get this. That means
we will manually tell claude code to perform the task, and it will do that by using the MCP.

First, we can install the claude code in our terminal globally, then we can tell claude code,
to bring the "mcp-playwright" code in our claude code system. Once it is installed, We can start the
claude code server from terminal and give the steps to perform. For example, we can provide the
steps manually and tell claude to create code based on the steps.

"""